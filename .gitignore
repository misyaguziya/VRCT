tmp/
build/
dist/
/tool-dist/
/annotation_jobs/
.venv-label-studio/
.venv-annotator/
/config.json
memo.txt
*.pyc
logs/
.venv/
.venv_cuda/
.venv_amd/
.venv-collector/
weights/
# Local evaluation dataset archives (never commit downloaded speech data)
common-voice-scripted-speech-*.tar.gz
.vscode
error.log
*.exe
*.ipynb
VRCT.zip
VRCT_cuda.zip

.qoder/

# Added by WebUI migration
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
.vscode
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.venv

# Customize
/build
error.txt
config.json
config.json.tmp


# Personal Claude Code settings (not shared with the team)
.claude/settings.local.json

# Claude Code environment setup (not committed)
CLAUDE.md
.claude/
docs/adr/
src-python/docs/ref/
src-python/telemetry_state.json

# OCR bubble-detector training data collected via tools/ocr_dataset_collector.py
# and tools/gemini_bbox_labeler.py (large binary screenshots, dev-machine only).
/dataset_collected/
/yolo_labels/

# Output of tools/amd_spike/check_amd.py (issue #88 の実機検証).
# 検証者のマシン構成が入るので、誤ってコミットしないように無視する。
/amd_spike_report.txt
/amd_spike_test_audio.wav

# bat/build_amd_check.bat の出力 (協力者へ渡す検証用 exe)。
# ROCm ランタイムを同梱するので展開 900MB / zip 248MB になる。
# 出力先を変えて逃げることがあるのでワイルドカードにしてある
# (何かがフォルダを掴んでいて PyInstaller が消せない状況が実際に起きた)。
/dist_amd_check*/
